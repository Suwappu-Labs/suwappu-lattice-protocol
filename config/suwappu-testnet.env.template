# SUWAPPU Testnet Configuration — RETIRED, DO NOT USE
#
# This template targeted the original SUWAPPU Testnet (Chain ID 103115120),
# whose RPC was an AWS load balancer in us-east-2. That infrastructure was torn
# down; the endpoint no longer resolves and the chain is unreachable. The RPC
# URL that used to live here has been removed so nobody copies a dead endpoint
# into a working .env and then debugs a connection error for an hour.
#
# Use `config/live-legs.env.template` instead — it targets the two live legs
# (Base Sepolia 84532, Ethereum Sepolia 11155111).
#
# Background: docs/plans/2026-08-24-testnet-cross-chain-relegging.md
# Addresses (historical record): docs/DEPLOYED_CONTRACTS.md
#
# Retained only so that older runbooks referencing this filename land on this
# explanation rather than a 404.

SUWAPPU_CHAIN_ID=103115120
# SUWAPPU_RPC_URL — intentionally omitted; the endpoint is gone.
SUWAPPU_ANCHOR_REGISTRY=0xB29d8BFF4973D1D7bcB10E32112EBB8fdd530bF4  # unreachable
