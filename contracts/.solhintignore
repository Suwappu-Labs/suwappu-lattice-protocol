# Dependencies
lib/
node_modules/
# Vendored third-party MPT code (Apache/MIT) — linted to upstream's standards,
# not ours; already excluded from slither (slither.config.json filter_paths).
src/vendor/
# Documented pre-existing baseline debt (reason-string / state-visibility warnings).
# Grandfathered so the pre-commit hook gates NEW code instead of perpetually
# failing on legacy — fix-forward and remove these two lines. (CLAUDE.md: secaudit
# baseline known-RED.)
src/LTPMultiSig.sol
src/SuwappuVault.sol
